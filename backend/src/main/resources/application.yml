spring:
  application:
    name: backend
    version: 0.0.0
  profiles:
    active: dev
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    properties:
      hibernate:
        default_batch_fetch_size: 100
    open-in-view: false

server:
  port: 8080
